import 'package:flutter/material.dart';

class AddAdsPage extends StatelessWidget {
  const AddAdsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text('تحت التطوير'),));
  }
}
